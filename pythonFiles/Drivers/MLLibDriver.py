import sys, pandas as pd, numpy as np
sys.path.append("pythonFiles")

from Functionality import MLLib as ml

#this function uses all the other functions in the library, so this is sufficent enough to 
#show that everything is working
ml.KNN()